{
    "rules": {
        "users": {
            "$uid": {
                ".read": "$uid === auth.uid",
                ".write": "$uid === auth.uid",
                "favorite_team": {
                    ".validate": "$uid === auth.uid"
                }
            }
        }
    }
}
